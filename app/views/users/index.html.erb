
<body>
    <%= render "layouts/profile", user: @user,book: @book %>
    <div class="row">
        <div class="col-md-9">
          <h2>Users</h2>
            <table class="table table-hover table-inverse">
             <thead>
              <tr>
                <th>image</th>
                <th>name</th>
                <th></th>
                <th colspan="3"></th>
              </tr>
             </thead>
              <tbody>
               <% @users.each do |user| %>
                 <tr colsapn="3">
                  <td>
                   <%= attachment_image_tag user, :profile_image, :fill, 40, 40, format: 'jpeg', class: "img-circle pull-left profile-thumb",fallback: "no_image.jpg",size:'40x40' %>
                  </td>
                  <td>
                    <%= user.name %>
                  </td>
                  <td><%= link_to "Show",user_path(user) %></td>
                <% end %>
               </tbody>
          　</table>
        </div>
   </div>
</body>
